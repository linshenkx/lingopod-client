name: Release Build

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        include:
          - os: ubuntu-latest
            output-name: linux
          - os: windows-latest
            output-name: windows

    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.4'
          channel: 'stable'
          cache: true
      
      - name: Install Dependencies
        run: flutter pub get

      # Web 构建
      - name: Build Web
        if: matrix.os == 'ubuntu-latest'
        run: |
          flutter config --enable-web
          flutter build web --release
          cd build/web
          zip -r ../../lingopod-web.zip ./*
        
      # Android APK 构建
      - name: Build Android APK
        if: matrix.os == 'ubuntu-latest'
        run: flutter build apk --release

      # Windows EXE 构建
      - name: Build Windows EXE
        if: matrix.os == 'windows-latest'
        run: flutter build windows --release
        
      # Linux 构建
      - name: Build Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y ninja-build libgtk-3-dev
          flutter config --enable-linux-desktop
          flutter build linux --release

      # 打包 Windows
      - name: Package Windows
        if: matrix.os == 'windows-latest'
        run: |
          cd build/windows/x64/runner/Release
          7z a -tzip ../../../../../lingopod-windows.zip ./*

      # 打包 Linux
      - name: Package Linux
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd build/linux/x64/release/bundle
          tar czf ../../../../../lingopod-linux.tar.gz ./*

      # 创建 Release
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            build/app/outputs/flutter-apk/app-release.apk
            lingopod-windows.zip
            lingopod-linux.tar.gz
            lingopod-web.zip
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            ## 下载
            - [Windows 客户端](lingopod-windows.zip)
            - [Linux 客户端](lingopod-linux.tar.gz)
            - [Android APK](app-release.apk)
            - [Web 版本](lingopod-web.zip)
            
            ## 部署说明
            ### Web 版本部署
            1. 解压 lingopod-web.zip 到 Web 服务器目录
            2. 配置服务器地址
            
            ### 桌面端
            1. 解压对应系统的压缩包
            2. 运行可执行文件
            3. 在设置中配置服务器地址
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
